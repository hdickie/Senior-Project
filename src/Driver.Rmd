---
title: "Driver"
author: "Hume Dickie"
date: "November 22, 2017"
output: html_document
---

```{r libs, include=FALSE}

```

```{r getData, include=FALSE}
#assumes this file is in the src folder
setwd("../data/First-Uploads")
first.dataset1 <- read.csv("dataset-1.csv")
first.top100news <- read.csv("top100news.csv")
```

This is the page that will be auto updated when new data is added. Here is proof!

```{r outputDataDimensions}
print(dim(first.dataset1))
print(dim(first.top100news))
```